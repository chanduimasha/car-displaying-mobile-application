<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.carsdisplaying">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CarsDisplaying">
        <activity
            android:label="Hints"
            android:parentActivityName=".MainActivity"
            android:name=".HintsActivity" />
        <activity
            android:name=".AdvancedLevelActivity"
            android:label="Advanced Level"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".IdentifyCarImageActivity"
            android:label="Identify The Car Image"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".IdentifyCarMakeActivity"
            android:label="Identify The Car Brand"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>